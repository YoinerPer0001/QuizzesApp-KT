<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.QuizzesIA" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Fondo completo de la pantalla -->
        <item name="windowSplashScreenBackground">@color/DarkBackGround</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash</item>

        <!-- Muy importante: transparente para evitar el círculo -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <item name="postSplashScreenTheme">@style/Theme.QuizzesIA</item>
    </style>

</resources>